{
  "name": "decision-wheel",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "scripts": {
    "dev": "pnpm --filter @decision-wheel/web dev",
    "build": "pnpm --filter @decision-wheel/web build",
    "preview": "pnpm --filter @decision-wheel/web preview",
    "typecheck": "pnpm --filter @decision-wheel/web exec tsc -b"
  }
}
